import React from 'react';
import { digiroom } from '../../../assets/images';

const LogoDigiroomCircle = (props) => {
  return <img src={digiroom.src} {...props} alt="digiroom logo" />;
};

export default LogoDigiroomCircle;
